{
  "name": "uft-learning-lounge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "UFT Learning Lounge — static site + Vercel serverless functions for the community Q&A (Groq answers, Prisma + Postgres storage, Resend email).",
  "scripts": {
    "postinstall": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
